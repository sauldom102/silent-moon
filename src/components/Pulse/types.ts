import { ViewStyle } from 'react-native';

export type Props = {
  circles: number;
  smallCircleSize: number;
  increaseCircleSize: number;
  style?: ViewStyle;
};

export type ContainerProps = {
  size: number;
};
