import { Source } from 'react-native-fast-image';

export type Props = {
  source: Source;
  color: string;
};

export type ContainerProps = {
  color: string;
};

export type ImageProps = {
  color: string;
  safeTop: number;
};
