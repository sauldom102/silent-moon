import { MainStackScreenProps } from 'navigator/types';

export type Props = MainStackScreenProps<'PlayOption'> & {};

export type ColorProps = {
  color: string;
};

export type ScrollProps = {
  safeBottom: number;
};
