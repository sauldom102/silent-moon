import { MainStackScreenProps } from 'navigator/types';

export type Props = MainStackScreenProps<'MusicPlayer'> & {};

export type BodyProps = {
  headerHeight: number;
};

export type ImageProps = {
  color?: string;
};
